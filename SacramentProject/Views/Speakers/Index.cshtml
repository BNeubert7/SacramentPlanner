@model IEnumerable<SacramentProject.Models.Speakers>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Topic)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.SacramentProgram)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Topic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SacramentProgram.MeetingDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SpeakerProgramId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SpeakerProgramId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SpeakerProgramId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
